#include <cs50.h>
#include <stdio.h>

int main(void)
{
     string names[] = {EMMA, RODRIGO, BRIAN, DAVID};

    for (int i = 0; i < (4); i++)
    {
        if (numbers[i] == 50)
        {
            printf("EMMA found!");
            return 1;
        }
    }
}